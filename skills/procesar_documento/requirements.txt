# Dependencias por formato (se importan de forma perezosa).
# TXT, MD y CSV no requieren nada externo.
pdfplumber>=0.11      # PDF
python-docx>=1.1      # Word (.docx)
openpyxl>=3.1         # Excel (.xlsx, .xls)
python-pptx>=0.6      # PowerPoint (.pptx)
pytesseract>=0.3      # OCR de imagenes (requiere binario 'tesseract' instalado)
Pillow>=10.0          # lectura de imagenes para OCR
anthropic>=0.39       # solo si se usa --resumen llm